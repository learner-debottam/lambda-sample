version: 0.2

phases:
  install:
    commands:
      - pip install aws-sam-cli
  build:
    commands:
      - sam build
      - sam package --s3-bucket targetflowbucket --output-template-file packaged.yaml
artifacts:
  type: zip
  files:
    - packaged.yaml
